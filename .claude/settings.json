{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./gradlew test --rerun-tasks >/dev/null 2>&1; C=$?; [ $C -eq 0 ] && S=PASS || S=\"FAIL(exit $C)\"; jq -cn --arg m \"[Stop 훅] 테스트 재실행 결과: $S\" '{hookSpecificOutput:{additionalContext:$m,systemMessage:$m}}'",
            "timeout": 600
          }
        ]
      }
    ]
  }
}
