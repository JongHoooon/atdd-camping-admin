# 회전 기록

4단계(acceptance-criteria → acceptance-test → implementation → verdict) 스킬 중 하나가
같은 실패를 세 번 반복하는 동안, 각 시도마다 무엇을 했는지 한 줄씩 남긴다(최대 3줄). 세 번째도
실패하면 멈추고 사람을 부른다. 정상적으로 완료됐을 때는 남기지 않는다 — 이건 완료 기록이
아니라 반복 실패 과정을 사람에게 넘길 때 보여주는 기록이다. 자동으로 검사되는 게이트는 아니다.

형식: `<시각> <티켓> <스킬>: <몇 번째 시도인지, 무엇을 했다가 어떻게 실패했는지>`

---

2026-08-25T20:58+0900 T-5 implementation: 1번째 시도 — 재고 음수 검증을 `stockQuantity < -100`
기준으로 추가했으나 임계값이 잘못돼(-1은 안 걸림) "재고를 음수로 수정하면 거부된다" 테스트가
여전히 실패(200, 기대 400).

2026-08-25T20:59+0900 T-5 implementation: 2번째 시도 — 재고 검증 코드를 지우고 가격 검증만
추가하는 실수(필드를 잘못 짚음). "재고를 음수로 수정하면 거부된다" 테스트가 여전히 실패
(200, 기대 400) — 같은 테스트가 두 번째로 막힘.

2026-08-25T20:59+0900 T-5 implementation: 3번째 시도 — 재고 음수 시 `IllegalStateException`을
던졌으나 전역 핸들러가 없어 500으로 변환됨(기대 400). "재고를 음수로 수정하면 거부된다"
테스트가 세 번째로 같은 이유(상태 코드 불일치)로 막혀 멈춤 — 사람에게 넘김.
